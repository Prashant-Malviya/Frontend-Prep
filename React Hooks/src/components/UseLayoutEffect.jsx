//  useLayoutEffect works similarly to useEffect , but it is called before the interface gets mounted.

// it means , useEffect get called after printing the dom elements. while
// useLayoutEffect gets called before printing the dom elements.